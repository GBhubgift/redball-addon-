# assets/images

The game is drawn entirely on a `<canvas>` and has no raster images. The only image asset (the enlarged SVG mouse cursor) stays inline in `game.js` for cross-browser `cursor: url(...)` compatibility.
